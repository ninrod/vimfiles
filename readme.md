# vimfiles: windows gvim config

## installation

* git clone this folder into `c:\Users\youruser\vimfiles`
* run `Set-ExecutionPolicy -ExecutionPolicy RemoteSigned` as admin.
* run the boostrap.ps1 script

